%h1 Listing posts

%table
  %tr
    %th Title
    %th Body
    %th Author
    %th
    %th
    %th

  - @posts.each do |post|
    %tr
      %td= post.title
      %td= raw post.body
      %td= post.admin.username
      %td= link_to "Show", post
      - if admin_signed_in?
        %td= link_to "Edit", edit_post_path(post)
        %td= link_to "Destroy", post, :confirm => "Are you sure?", :method => :delete

%br

= link_to "New post", new_post_path
